{
  "name": "simple-browser",
  "version": "1.0.10",
  "type": "module",
  "files": [
    "./build/",
    "./run"
  ],
  "bin": {
    "simple-browser": "run",
    "browser": "run"
  },
  "scripts": {
    "main": "electron ./build/main.js",
    "dev": "npx --yes nodemon@latest --watch src --ext ts --exec 'tsc && NODE_ENV=development REMOTE_DEBUGGING_PORT=9222 npm run main'",
    "start": "NODE_ENV=production npm run main",
    "prepublish": "tsc"
  },
  "dependencies": {
    "electron": "^42.1.0",
    "find-free-ports": "^3.1.1"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "typescript": "^6.0.3"
  }
}